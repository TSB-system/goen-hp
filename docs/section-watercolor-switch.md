# セクション別水彩切り替え

2026-09-07 検証。単一の連続作品から、各セクションに対応する既存水彩画像への切り替えを実装。

- PC 11セクション、モバイルは存在する8セクションを対象。
- 画像パス: assets/images/section-watercolors/{section-id}-v1.webp。モバイルは -v1-900.webp。
- JS: assets/js/goen-watercolor-sections.js。画面上から34%の位置を基準にセクションを判定し、画像読み込み完了後に切り替える。
- 読み込み中は前の絵を維持。古い非同期処理の上書きを防止し、次の画像を先読みする。
- 背景は画面横幅100%、高さは画像比率で自動設定。縦に引き伸ばさないため、特にモバイルでは上下に白い余白が残る。
- 元のTOP・動画・セクション本文・リンクは変更しない。
- ローカル検証: _tools/check_watercolor_switch.py。1440/1920/2560/390/375幅、全セクションの往復・高速移動、画像横幅、404、横はみ出しを確認。
- 2026-09-07、ユーザーの「反映してください」を受け、本番公開を実行。公開前コミットは56ba32f。
